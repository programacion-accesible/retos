{
	"name": "retos",
	"version": "1.0.0",
	"description": "Automatización de estadísticas",
	"main": "generateStats.js",
	"scripts": {
		"build": "node generateStats.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"fs": "^0.0.1-security"
	}
}
